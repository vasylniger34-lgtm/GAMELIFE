# 💾 Система збереження даних - Game Life PWA

## 📋 Огляд

Реалізовано повнофункціональну систему локального збереження з автозбереженням, експортом/імпортом, версіонуванням та валідацією даних.

---

## ✅ Реалізовані функції

### 1. **Локальне збереження (Persistent Storage)**

- ✅ Використовується **Zustand persist middleware** для автоматичного збереження в `localStorage`
- ✅ Зберігаються всі ключові модулі:
  - `currentStats` - поточні стати активного дня
  - `days` - всі дні з історією
  - `quests` - всі квести
  - `quickActions` - швидкі дії
  - `shopItems` - товари магазину
  - `profile` - профіль гравця (рівень, XP)
  - `diamonds` - поточна кількість діамантів
  - `diamondsEarnedTotal` - загальна кількість зароблених діамантів
  - `timeMeta` - метадані часу
  - `purchaseHistory` - історія покупок
  - `achievements` - досягнення
  - `lastDayStarted` - остання дата старту дня

- ✅ Автоматичне збереження при кожній зміні стану через `set()`
- ✅ Використовується `partialize` для збереження тільки даних (без функцій)

### 2. **Автозбереження (Autosave)**

- ✅ Автоматичне збереження кожні **3 хвилини** (налаштовується)
- ✅ Збереження при закритті вкладки/вікна (`beforeunload`)
- ✅ Збереження при перезавантаженні сторінки (`pagehide`)
- ✅ Збереження при старті нового дня (`startDayWithInitialStats`)
- ✅ **Неблокуюче збереження** через `requestIdleCallback` або `setTimeout`
- ✅ Індикатор збереження в UI (компонент `SaveIndicator`)

### 3. **Експорт / Імпорт**

#### Експорт:
- ✅ Кнопка "Експортувати дані" в профілі
- ✅ Експорт у форматі JSON з читабельним форматуванням
- ✅ Включає версію, timestamp та checksum для валідації
- ✅ Автоматичне завантаження файлу з назвою `game-life-save-YYYY-MM-DD.json`

#### Імпорт:
- ✅ Кнопка "Імпортувати дані" в профілі
- ✅ Вибір файлу через `<input type="file">`
- ✅ Валідація структури перед імпортом
- ✅ Підтвердження перед заміною поточних даних
- ✅ Автоматичне оновлення UI після імпорту
- ✅ Обробка помилок з зрозумілими повідомленнями

### 4. **Версіонування та міграції**

- ✅ Версія збереження: **3**
- ✅ Автоматичні міграції при оновленні версії:
  - **Версія 1 → 2**: Додано поля для досягнень, історії покупок, `lastDayStarted`
  - **Версія 2 → 3**: Додано поля для тем дня, головного квесту, другого шансу, XP та діамантів за день
- ✅ Зворотна сумісність для мінорних змін
- ✅ Безпечна міграція без втрати даних

### 5. **Валідація та обробка помилок**

- ✅ Валідація структури при завантаженні стану
- ✅ Перевірка наявності всіх обов'язкових полів
- ✅ Перевірка типів даних
- ✅ Checksum для додаткової перевірки цілісності
- ✅ Fallback до дефолтних значень при пошкодженні даних
- ✅ Обробка помилок без краша додатку
- ✅ Зрозумілі повідомлення про помилки для користувача

### 6. **UX покращення**

- ✅ **Індикатор збереження** (`SaveIndicator`):
  - Показує статус збереження (збереження/збережено/простою)
  - Анімації та візуальний фідбек
  - Розташований у правому нижньому куті (вище навігації)
  
- ✅ **Повідомлення про успіх/помилку**:
  - Alert при експорті/імпорті
  - Підтвердження перед імпортом
  
- ✅ **Неблокуюче збереження**:
  - Використання `requestIdleCallback` для оптимізації
  - Fallback на `setTimeout` для сумісності
  - Немає затримок UI під час збереження

---

## 📁 Структура файлів

### Нові файли:

1. **`src/services/saveSystem.ts`**
   - Експорт/імпорт стану
   - Валідація структури
   - Checksum обчислення
   - Типи для експортованого стану

2. **`src/services/autosave.ts`**
   - Автоматичне збереження
   - Управління інтервалами
   - Обробка подій закриття вікна

3. **`src/components/SaveIndicator.tsx`**
   - Візуальний індикатор збереження
   - Статуси: збереження/збережено/простою

### Оновлені файли:

1. **`src/state/store.ts`**
   - Додано міграції для версій 1→2 та 2→3
   - Додано `partialize` для оптимізації збереження
   - Версія збереження: 3

2. **`src/pages/Profile.tsx`**
   - Додано секцію "Збереження даних"
   - Кнопки експорту/імпорту
   - Кнопка ручного збереження

3. **`src/components/layout/Header.tsx`**
   - Інтегровано `SaveIndicator`

4. **`src/App.tsx`**
   - Ініціалізація автозбереження при старті додатку

5. **`src/styles.css`**
   - Стилі для індикатора збереження
   - Стилі для кнопок збереження

---

## 🔧 Технічні деталі

### Формат збереження:

```typescript
interface ExportedState {
  version: number;           // Версія формату (3)
  timestamp: string;         // ISO timestamp експорту
  checksum?: string;        // Опціональний checksum
  data: {
    currentStats: DailyStats;
    days: Record<string, Day>;
    quests: Record<string, Quest>;
    quickActions: Record<string, QuickAction>;
    shopItems: Record<string, ShopItem>;
    profile: Profile;
    diamonds: number;
    diamondsEarnedTotal: number;
    timeMeta: TimeMeta;
    purchaseHistory: PurchaseRecord[];
    achievements: Record<string, Achievement>;
    lastDayStarted: string | null;
  };
}
```

### Автозбереження:

- **Інтервал**: 3 хвилини (налаштовується в `AUTOSAVE_INTERVAL_MS`)
- **Події**: `beforeunload`, `pagehide`
- **Метод**: `requestIdleCallback` (fallback: `setTimeout`)

### Міграції:

Міграції виконуються автоматично при завантаженні стану зі старішої версії:

```typescript
migrate: (persistedState: any, version: number) => {
  // Версія 1 → 2
  if (version < 2) {
    // Додаємо нові поля
  }
  
  // Версія 2 → 3
  if (version < 3) {
    // Додаємо нові поля
  }
  
  return persistedState;
}
```

---

## 🎯 Використання

### Експорт даних:

1. Відкрити **Профіль**
2. Знайти секцію **"Збереження даних"**
3. Натиснути **"💾 Експортувати дані"**
4. Файл автоматично завантажиться

### Імпорт даних:

1. Відкрити **Профіль**
2. Знайти секцію **"Збереження даних"**
3. Натиснути **"📥 Імпортувати дані"**
4. Вибрати JSON файл збереження
5. Підтвердити імпорт
6. Дані автоматично оновляться

### Ручне збереження:

1. Відкрити **Профіль**
2. Знайти секцію **"Збереження даних"**
3. Натиснути **"💾 Зберегти зараз"**

---

## 🔒 Безпека та надійність

- ✅ Валідація структури перед імпортом
- ✅ Checksum для перевірки цілісності
- ✅ Fallback до дефолтних значень при помилках
- ✅ Обробка помилок без краша додатку
- ✅ Підтвердження перед заміною даних
- ✅ Версіонування для сумісності

---

## 📊 Продуктивність

- ✅ Неблокуюче збереження через `requestIdleCallback`
- ✅ Оптимізоване збереження через `partialize` (тільки дані)
- ✅ Автозбереження кожні 3 хвилини (налаштовується)
- ✅ Мінімальний вплив на UI

---

## 🚀 Майбутні покращення

Можливі додаткові функції:

- [ ] Хмарне резервне копіювання
- [ ] Історія збережень (кілька версій)
- [ ] Автоматичне резервне копіювання
- [ ] Шифрування збережень
- [ ] Синхронізація між пристроями
- [ ] Налаштування інтервалу автозбереження в UI

---

## ✅ Висновок

Реалізовано повнофункціональну систему збереження з усіма вимогами:

1. ✅ Локальне збереження через Zustand persist
2. ✅ Автозбереження кожні 3 хвилини та при важливих подіях
3. ✅ Експорт/імпорт JSON з валідацією
4. ✅ Версіонування та міграції
5. ✅ Валідація та обробка помилок
6. ✅ UX покращення з індикатором збереження

Система готова до використання та забезпечує надійне збереження всіх даних користувача.
